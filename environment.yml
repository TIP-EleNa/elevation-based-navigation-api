name: elena-ultra-api  # overridden at install, so this is for your use with conda env
channels:
  - conda-forge
dependencies:
  - osmnx
  - googlemaps
  - requests
  - numpy
  - networkx
  - flask
  - flask-cors